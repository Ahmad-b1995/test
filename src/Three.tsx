function Three({componentThreeOnClick}) {
  return (
    <div>
      hello, I am component number THREE
      <button onClick={componentThreeOnClick}>go to page one</button>
    </div>
  );
}

export default Three;
